const express = require("express");

const { register, login } = require("../../controllers/Api/deliveryController");

const router = express.Router();

//router.post("/add_address", address_store);


module.exports = router;
